import { Field } from 'formik';
import styled from 'styled-components';

export const FilterPlateStyled = styled.section`
  border: 3px solid #4b339b;
  padding: 8px;
  margin-bottom: 16px;
`;

export const Input = styled(Field)`
  color: #2a2a2a;
  font-size: 16px;
`